# NEON (POSTGRESQL) CONFIGURATION - Uses Render Environment Variables
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# HIBERNATE/JPA SETTINGS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# SERVER CONFIGURATION
server.port=${SERVER_PORT:8080}
# We keep these for now, but they will be ignored once the SessionCreationPolicy is STATELESS
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true
server.forward-headers-strategy=native

# 🚨 JWT CONFIGURATION ADDED 🚨
jwt.secret=ThisIsAVeryLongAndSecureSecretKeyForSigningYourTokens1234567890ABCDEF
jwt.expiration=86400000 # 24 hours in milliseconds